



{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Static website",
  "scripts": {
    "build": "mkdir -p .vercel/output/static && cp index.html .vercel/output/static/"
  },
  "private": false
}
