{
  "name": "static-cloudflare-site",
  "version": "1.0.0",
  "description": "Static website for Cloudflare Pages",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed for static site'"
  }
}
